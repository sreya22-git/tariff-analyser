# Copy to .env and fill in to enable the agentic/LLM layer.
# Without these set, the app falls back to deterministic heuristics/stubs and remains fully demoable.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
